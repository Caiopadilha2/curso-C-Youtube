#include <stdio.h>
#include <stdlib.h>

int main(){
    // Declarações
    float adicao, subtracao, multiplicacao, divisao;
    
    adicao = 36 + 24;
    subtracao = 20 - 10;
    multiplicacao = 2 * 10;
    divisao = 36.0 / 41.0;
    
    printf("Adicao: %f \n", adicao);
    printf("Subtracao: %f \n", subtracao);
    printf("Multiplicacao: %f \n", multiplicacao);
    printf("Divisao: %f \n", divisao);
    
    
    system("pause");
    return 0;
}
